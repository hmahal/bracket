# Beatiful Racket Brainfuck

To install, run `raco pkg install` and it should be available globally to Racket as `bf`.

To use, include `#lang/bf` at the top of the racket file. See `examples` directory for sample code.
